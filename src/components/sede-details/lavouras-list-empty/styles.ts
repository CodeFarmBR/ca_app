import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
	emptyListContainer: {
		alignItems: "center",
		position: "absolute",
		left: 0,
		right: 0,
		top: "35%",
		paddingHorizontal: 16,
	},
})
